import { makeStyles } from "@fluentui/react-components";

export const useProfileLanguageSubmenuStyles = makeStyles({
  languageSelection: {
    overflowY: "auto",
    overflowX: "hidden",
    minHeight: "32px",
    maxHeight: "30vh",
  },
});
