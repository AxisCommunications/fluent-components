import { Canvas, Description, Meta, Subtitle, Title } from "@storybook/blocks";
import * as PasswordInputStories from "./password-input.stories";

<Meta of={PasswordInputStories} />

<Title />
<Subtitle>Password field with built-in visibility toggle.</Subtitle>
<Description>
PasswordInput wraps the Fluent input behavior for sensitive values and provides a
standard reveal/hide interaction.
</Description>

## Exports

- PasswordInput

## Installation

```bash
pnpm add @axiscommunications/fluent-password-input
```

## Example

<Canvas of={PasswordInputStories.Default} />

## Guidance

- Use for credentials and other sensitive text values.
- Pair with form validation and helper/error text in parent forms.
- Avoid pre-populating password fields unless explicitly required.
